import { FilteredListPipePipe } from './filtered-list-pipe.pipe';

describe('FilteredListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
