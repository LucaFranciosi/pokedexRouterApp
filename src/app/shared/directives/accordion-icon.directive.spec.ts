import { AccordionIconDirective } from './accordion-icon.directive';

describe('AccordionIconDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionIconDirective();
    expect(directive).toBeTruthy();
  });
});
